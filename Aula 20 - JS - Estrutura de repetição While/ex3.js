const io = require('../io/io')
/* Programa de computador para escrever Javascript por 20 vezes */
let iterador = 1

while (iterador <= 20) {//laço ou looping
    io.write(`Javascript`)
iterador ++ //altera o valor do iterador (soma + 1)
}

