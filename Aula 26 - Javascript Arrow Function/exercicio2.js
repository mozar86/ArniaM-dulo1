const io = require('../io/io')

/* Faça um programa que receba do usuário um array de números. Faça uma função que recebe o array de números e retorna a soma desses números. A soma deve ser mostrada ao usuário. */

let numero

const somaNumeros = (numero) => {
    let numeros = []

    for (let i = 0; i <= 5; i++) {
        io.write('Digite um número:')
        io.readInt(numero[i])
        numeros.push(i)
    }
    
    for () {

    }

    return
}