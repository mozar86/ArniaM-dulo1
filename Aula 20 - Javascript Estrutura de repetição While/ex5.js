const io = require('../io/io')
/* Programa de computador para escrever um nome fornecido pelo usuário 10 vezes */
let iterador = 0
while (iterador <= 15) {//laço ou looping
    io.write('Javascript')
iterador = iterador ++ //altera o valor do iterador (soma + 1)
}